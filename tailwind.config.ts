import type { Config } from "tailwindcss"

const config: Config = {
  theme: {
    extend: {
      colors: {
        "mariage-blue": "#7287B1", // 👈 ta couleur personnalisée
      },
    },
  },
}

export default config
